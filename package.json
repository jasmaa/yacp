{
  "private": true,
  "name": "yacp",
  "version": "1.0.0",
  "description": "Yet another CORS proxy",
  "main": "index.js",
  "scripts": {
    "start": "concurrently -k \"rescript build -w\" \"wrangler dev\"",
    "build": "rescript",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "prettier": "^1.18.2",
    "rescript": "^9.1.4"
  }
}
